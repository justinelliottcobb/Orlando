#!/usr/bin/env bash
# Pre-commit hook for Orlando transducer library
# Runs formatting, linting, and tests before allowing commits

set -e  # Exit on any error

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}🔍 Running pre-commit checks...${NC}\n"

# Check if we're on WASM target (if so, skip native tests)
NATIVE_TARGET="x86_64-unknown-linux-gnu"

# 1. Rustfmt - Code formatting
echo -e "${YELLOW}1. Running rustfmt...${NC}"
if ! cargo fmt --all -- --check; then
    echo -e "${RED}❌ Formatting check failed!${NC}"
    echo -e "${YELLOW}💡 Run 'cargo fmt --all' to fix formatting${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Formatting check passed${NC}\n"

# 2. Clippy - Linting (Native target)
echo -e "${YELLOW}2a. Running clippy (native)...${NC}"
if ! cargo clippy --all-targets --all-features --target "$NATIVE_TARGET" -- -D warnings; then
    echo -e "${RED}❌ Clippy check failed on native target!${NC}"
    echo -e "${YELLOW}💡 Fix clippy warnings before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Clippy check passed (native)${NC}\n"

# 2b. Clippy - Linting (WASM target)
echo -e "${YELLOW}2b. Running clippy (wasm32)...${NC}"
if ! cargo clippy --lib --target wasm32-unknown-unknown -- -D warnings; then
    echo -e "${RED}❌ Clippy check failed on WASM target!${NC}"
    echo -e "${YELLOW}💡 Fix WASM-specific clippy warnings before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Clippy check passed (wasm32)${NC}\n"

# 3. Unit tests
echo -e "${YELLOW}3. Running unit tests...${NC}"
if ! cargo test --lib --target "$NATIVE_TARGET" --quiet; then
    echo -e "${RED}❌ Unit tests failed!${NC}"
    echo -e "${YELLOW}💡 Fix failing tests before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Unit tests passed${NC}\n"

# 4. Documentation tests
echo -e "${YELLOW}4. Running documentation tests...${NC}"
if ! cargo test --doc --target "$NATIVE_TARGET" --quiet; then
    echo -e "${RED}❌ Documentation tests failed!${NC}"
    echo -e "${YELLOW}💡 Fix failing doctests before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Documentation tests passed${NC}\n"

# 5. Integration tests (quick mode - skip heavy property tests)
echo -e "${YELLOW}5. Running integration tests...${NC}"
if ! cargo test --test integration --target "$NATIVE_TARGET" --quiet; then
    echo -e "${RED}❌ Integration tests failed!${NC}"
    echo -e "${YELLOW}💡 Fix failing integration tests before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Integration tests passed${NC}\n"

# 6. WASM build check
echo -e "${YELLOW}6. Running WASM build check...${NC}"
if ! cargo build --lib --target wasm32-unknown-unknown --quiet; then
    echo -e "${RED}❌ WASM build failed!${NC}"
    echo -e "${YELLOW}💡 Fix WASM build errors before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ WASM build check passed${NC}\n"

# 7. Native build check
echo -e "${YELLOW}7. Running native build check...${NC}"
if ! cargo build --target "$NATIVE_TARGET" --quiet; then
    echo -e "${RED}❌ Native build failed!${NC}"
    echo -e "${YELLOW}💡 Fix build errors before committing${NC}\n"
    exit 1
fi
echo -e "${GREEN}✅ Native build check passed${NC}\n"

echo -e "${GREEN}✨ All pre-commit checks passed!${NC}"
echo -e "${BLUE}📝 Proceeding with commit...${NC}\n"

exit 0
